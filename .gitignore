# Local dev artifacts
.venv/
__pycache__/
*.pyc

# Scraper CSV output — only the .xlsx dashboards are committed (see workflows)
ISCC certificates*.csv
SURE certificates*.csv
PEFC certificates*.csv
FSC certificates*.csv
GGL certificates*.csv
SBP certificates*.csv

# Email-digest build artifacts
csv_export/
certificate-csvs-*.zip

# Combined workbook — the full "latest" plus the single newest dated copy are
# committed (the email links to the dated file so the download carries a date;
# monthly-build prunes the previous dated copy). The slim dashboard-only copy
# and the transient CSV stay local / CI-only.
All certificates (dashboard) latest.xlsx
All certificates latest.csv

# Debug scratch
_pefc_*.html
