name: Monthly FSC Certificate Scraper

on:
  schedule:
    - cron: '0 9 1 * *'   # 09:00 UTC on the 1st of every month (after ISCC/SURE/PEFC)
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install --quiet -r requirements.txt

      - name: Run scraper
        run: python scraper_fsc.py

      - name: Generate Excel dashboard
        run: python generate_excel_fsc.py

      - name: Commit and push Excel files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git rm -f --ignore-unmatch "FSC certificates"*.csv
          git add "FSC certificates"*.xlsx
          git diff --cached --quiet && echo "No changes to commit." && exit 0
          git commit -m "chore: monthly FSC certificate scrape $(date -u +%Y-%m-%d)"
          git push
